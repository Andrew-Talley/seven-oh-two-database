query getSingleTournamentView($id: Int) {
  tournament(tournamentId: $id) {
    name
    start
    end
    host
    
    teams {
      wins
      ties
      losses

      cs
      ocs
      pd
    }
  }
}